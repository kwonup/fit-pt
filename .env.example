# 루트 .env.example
# 각 앱의 환경변수는 아래 위치에 설정합니다:
#   Frontend: apps/web/.env.local  (apps/web/.env.local.example 참고)
#   Backend:  apps/api/.env        (apps/api/.env.example 참고)
#
# 이 파일은 구조 안내용입니다. 실제 값은 각 앱 폴더의 .env 파일에 설정하세요.
